# Port-Scanner
Simple Python Port Scanner

This project is a simple and educational **Python port scanner** built with the `python-nmap` library.  
It allows you to scan ports on your own machine or any target you have legal permission to scan.

---

## 🚀 Features

- Scan a specific IP address  
- Scan a range of ports (default: 1–1024)  
- Detect open/closed ports  
- Display results in a clear and readable format  
- Uses the official `nmap` engine through the `python-nmap` library  

---

## 📌 Requirements

Before running the script, make sure you have:

### 1. Python installed  
(Version 3.10+ recommended)

### 2. The `python-nmap` library  
Install it with:

```bash
pip install python-nmap
